{
  "name": "code-review-ai",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend & npm run dev --workspace=backend",
    "convex": "npx convex dev"
  },
  "dependencies": {
    "convex": "^1.31.3"
  }
}
